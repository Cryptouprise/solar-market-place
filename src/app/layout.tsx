import type { Metadata } from 'next';
import './globals.css';
import Navbar from '@/components/layout/Navbar';
import Footer from '@/components/layout/Footer';

export const metadata: Metadata = {
  title: 'Solar Market - Premium Solar Panels & Renewable Energy Solutions',
  description:
    'Find the best solar panels and renewable energy solutions at competitive prices. Shop from trusted sellers with warranty protection.',
  keywords: 'solar panels, renewable energy, solar marketplace, photovoltaic, clean energy',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className="antialiased min-h-screen flex flex-col">
        <Navbar />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
